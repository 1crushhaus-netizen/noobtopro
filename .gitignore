# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# test coverage output
/coverage

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — ignore every .env* (e.g. .env, .env.local, .env.production)
# so a real-secret file can't be committed, but keep the checked-in template.
.env*
!.env.example

# vercel
.vercel

# claude code (local session/scheduler artifacts)
.claude/

# typescript
*.tsbuildinfo
next-env.d.ts
